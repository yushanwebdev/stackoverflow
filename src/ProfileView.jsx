const ProfileView = (props) => {
  return (
    <>
      <div id="profile" ref={props.profileCompRef}>
        <strong>Profile</strong>
      </div>
    </>
  );
};

export default ProfileView;
